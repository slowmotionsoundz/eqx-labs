/**
 * @file Settings module for EQX Admin CRM.
 * Settings controls are configured statically within HTML markup.
 */

export function renderSettings() {
  // Settings view is pre-rendered in HTML markup
}
